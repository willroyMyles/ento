import 'package:flutter/material.dart';

class CompaniesDetailView extends StatelessWidget {
  const CompaniesDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
